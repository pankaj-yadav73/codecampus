export interface USER {
  name: string;
  email: string;
  clerkUserId: string;
  image: string;
}

export interface COURSES {
  tittle: string;
  description: string;
  image: string;
  videoFile: string;
  instructorId: string;
}
